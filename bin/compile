#!/usr/bin/env bash

set -e            # fail fast
set -o pipefail   # don't ignore exit codes when piping output

# Configure directories
build_dir=$1
cache_dir=$2
env_dir=$3

export "DATABASE_URL=$(cat $env_dir/DATABASE_URL)"

echo "Running pre-deploy script"

cd $build_dir && sh ./scripts/pre-deploy

